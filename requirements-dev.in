# Herramientas de desarrollo — no van a producción
ruff>=0.4
pre-commit>=3.7
pytest>=8.0
pytest-cov>=5.0
boto3>=1.34          # para tests locales que mockean AWS
moto>=5.0            # librería para mockear servicios AWS en tests
